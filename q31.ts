//question no 31
/*No Users: Add an if test to Exercise 28 to make sure the list of
 users is not empty. */

 let user=["adam","eric","admin","john"];

 if(user.length==0){
    console.log("no user found");
 } 
   else {
    user=[];
    console.log("we need to find some new users.");
 }